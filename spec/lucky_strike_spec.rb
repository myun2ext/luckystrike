require 'spec_helper'

describe LuckyStrike do
  it 'has a version number' do
    expect(LuckyStrike::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
